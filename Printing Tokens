#include<stdio.h>
int main()
{
    char s[10000];
    int i;
    scanf(" %[^\n]", &s);

    for(i=0;s[i]!='\0';i++)
    {
        printf("%c",s[i]);

        if(s[i]==' ')
        {
            printf("\n");
        }
    }
    return 0;
}
